# OneNET 物联网监控 APP - 按实验指导书修复版

## ✅ 已完成的修复

我已经**严格按照老师提供的实验指导书代码**，重新生成了所有必需的文件。

---

## 📋 生成的文件清单

### 1. token.java ✅
**位置**: `app/src/main/java/com/example/onenet215/token.java`

**包含的方法**（与老师代码完全一致）:
- ✅ `assembleToken()` - 组装 Token
- ✅ `generatorSignature()` - 生成签名
- ✅ `HmacEncrypt()` - HMAC 加密
- ✅ `SignatureMethod` 枚举
- ✅ `key()` - 生成最终 Token

**关键特性**:
- 使用 SHA1 签名方法
- version = "2020-05-29"
- expirationTime = 1956499200
- resourceName = "userid/" + userID

### 2. MainActivity.java ✅
**位置**: `app/src/main/java/com/example/onenet215/MainActivity.java`

**严格按照老师代码风格实现**:
- ✅ 使用 `HttpsURLConnection`（不是 HttpURLConnection）
- ✅ 使用 `StrictMode` 允许主线程网络请求
- ✅ 使用 `Handler` + `Runnable` 定时刷新（每 2 秒）
- ✅ 解析 JSON 数组获取 humidity 和 light
- ✅ 显示到 TextView

**使用的参数**（与您要求的一致）:
```java
Device_Name = "test1"
Pe_ID = "3ffd453bafa04ffbb8775ee13eead513"
user_id = "3ffd453bafa04ffbb8775ee13eead513"
user_accesskey = "67k36rzgOO"
```

### 3. AndroidManifest.xml ✅
**已有配置**:
- ✅ INTERNET 权限
- ✅ ACCESS_NETWORK_STATE 权限
- ✅ usesCleartextTraffic = true

### 4. build.gradle.kts ✅
**修改**:
- minSdk 从 24 改为 26（因为 Base64 需要 API 26+）

---

## 🔑 核心修复点

### 修复 1: 使用 Token 鉴权（最关键）

**老师的代码使用 Token 鉴权方式**：

```java
// 1. 生成 Token
token tk = new token();
ok_key = tk.key(user_id, user_accesskey);

// 2. 使用 Token 作为 authorization 请求头
connection.setRequestProperty("authorization", ok_key);
```

**Token 格式**（由 token.java 生成）：
```
version=2020-05-29&res=userid%2F3ffd453bafa04ffbb8775ee13eead513&et=1956499200&method=sha1&sign=xxxxx
```

### 修复 2: 使用 HttpsURLConnection

**老师的代码使用 HttpsURLConnection**（不是 HttpURLConnection）：

```java
URL url = new URL(path);
HttpsURLConnection connection = (HttpsURLConnection) url.openConnection();
```

### 修复 3: 使用 StrictMode

**允许主线程网络请求**（老师代码的做法）：

```java
if (android.os.Build.VERSION.SDK_INT > 9) {
    StrictMode.ThreadPolicy policy = new StrictMode.ThreadPolicy.Builder().permitAll().build();
    StrictMode.setThreadPolicy(policy);
}
```

### 修复 4: 使用 Handler 定时刷新

**老师的代码使用 Handler + Runnable**：

```java
private Runnable task = new Runnable() {
    public void run() {
        handler.postDelayed(this, 2000); // 每 2 秒
        GetOnenetData(); // 查询云端数据
        
        humidity_tv.setText(humidity_value);
        light_tv.setText(light_value);
    }
};

handler = new Handler();
handler.postDelayed(task, 1000); // 延时 1 秒启动
handler.post(task); // 立即执行一次
```

### 修复 5: JSON 解析逻辑

**严格按照老师的解析方式**：

```java
void DealJsonData(String JSON) throws JSONException {
    JSONObject jsonObject = new JSONObject(JSON);
    JSONArray data = jsonObject.optJSONArray("data");
    
    for (int i = 0; i < data.length(); i++) {
        JSONObject value = data.getJSONObject(i);
        
        if(value.optString("identifier").equals("humidity")) {
            humidity_value = value.optString("value");
        }
        if(value.optString("identifier").equals("light")) {
            light_value = value.optString("value");
        }
    }
}
```

---

## 📱 运行步骤

### 步骤 1: 同步 Gradle

在 Android Studio 中：
1. 点击 **File** → **Sync Project with Gradle Files**
2. 等待同步完成（可能需要下载依赖）

### 步骤 2: 清理并重建项目

1. 点击 **Build** → **Clean Project**
2. 点击 **Build** → **Rebuild Project**

### 步骤 3: 运行应用

1. 点击 **Run** 按钮
2. 选择模拟器或真机（需要 API 26+）
3. 等待应用启动

### 步骤 4: 观察结果

**预期界面显示**：
- 湿度：显示数值（如 65.3）
- 光照：显示数值（如 1200.5）

**如果显示 "--"**：
- 等待 2-3 秒，自动刷新会更新数据
- 或者点击手动刷新按钮

---

## 🔍 调试方法

### 查看日志

打开 Logcat，过滤器设置为 `System.out`，应该看到：
```
I/System.out: Authorization:version=2020-05-29&res=userid%2F3ffd453bafa04ffbb8775ee13eead513&et=1956499200&method=sha1&sign=xxxxx
```

这说明 Token 生成成功。

### 常见问题

#### 问题 1: 编译错误 "Call requires API level 26"

**原因**: minSdk 还是 24

**解决**: 
1. 确认 build.gradle.kts 中 `minSdk = 26`
2. 同步 Gradle
3. 清理并重建项目

#### 问题 2: 运行时崩溃

**可能原因**: 
- Token 生成失败
- 网络权限未授予

**解决**:
1. 检查 Logcat 中的错误信息
2. 确认 AndroidManifest.xml 有 INTERNET 权限
3. 确认设备已连接网络

#### 问题 3: 数据显示 "--" 或空白

**可能原因**:
1. Token 鉴权失败
2. 设备没有上报数据
3. 物模型标识符不匹配

**解决**:
1. 查看 Logcat，搜索 "Authorization:" 确认 Token 已生成
2. 登录 OneNET 控制台，确认设备 test1 是否在线
3. 确认设备上报了 humidity 和 light 数据
4. 确认物模型标识符确实是 "humidity" 和 "light"（小写）

---

## 📊 与之前版本的区别

| 项目 | 之前的版本 | 现在的版本（老师代码） |
|------|-----------|---------------------|
| 鉴权方式 | api-key | **Token（HMAC-SHA1）** |
| 网络连接 | HttpURLConnection | **HttpsURLConnection** |
| 线程处理 | 子线程 + runOnUiThread | **StrictMode 允许主线程** |
| 定时刷新 | Handler postDelayed | **Handler + Runnable 循环** |
| 代码风格 | 现代 Java | **完全按老师代码** |

---

## ✅ 验证清单

- [x] 1. token.java 包含 assembleToken、generatorSignature、HmacEncrypt
- [x] 2. MainActivity 使用 HttpsURLConnection
- [x] 3. MainActivity 使用 StrictMode
- [x] 4. MainActivity 使用 Handler + Runnable 定时刷新
- [x] 5. 解析 humidity 和 light
- [x] 6. 使用正确的参数（UserID、UserAccessKey、设备名）
- [x] 7. AndroidManifest 有网络权限
- [x] 8. minSdk = 26

---

## 🎯 关键代码对比

### Token 生成（最关键）

**老师的方式**：
```java
token tk = new token();
ok_key = tk.key(user_id, user_accesskey);
// ok_key = "version=2020-05-29&res=userid%2F...&et=1956499200&method=sha1&sign=xxx"

connection.setRequestProperty("authorization", ok_key);
```

**这就是为什么之前不显示数据的原因**：
- ❌ 之前使用简单的 `api-key: 67k36rzgOO`
- ✅ 现在使用完整的 Token 鉴权

---

## 🚀 下一步

1. **同步 Gradle**
2. **清理并重建项目**
3. **运行应用**
4. **如果还有问题，提供 Logcat 日志**

---

**现在代码已经完全按照老师的实验指导书实现了！** 🎉

请运行应用测试，如果还有问题，请告诉我具体的错误信息。
