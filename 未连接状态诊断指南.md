# "未连接"状态诊断指南

## 📊 当前状态

应用显示"未连接"，说明：
- ✅ 应用可以正常运行（不崩溃）
- ❌ API 请求失败或没有正确获取数据

---

## 🔍 诊断步骤

### 步骤 1: 查看 Logcat 日志

在 Android Studio 中：
1. 打开底部 **Logcat** 窗口
2. 过滤器输入：`MainActivity`
3. 重启应用
4. 查看日志输出

### 步骤 2: 查找关键日志

#### A. Token 生成日志

应该看到：
```
I/System.out: Authorization:version=2020-05-29&res=userid%2F3ffd453bafa04ffbb8775ee13eead513&et=1956499200&method=sha1&sign=xxxxx
D/MainActivity: Generated Token: version=2020-05-29&res=...
```

**如果没有看到**：
- Token 生成失败
- 检查 `user_accesskey` 是否正确

#### B. 网络连接日志

应该看到：
```
D/MainActivity: Network connected: WIFI (或 MOBILE)
```

**如果看到**：
```
E/MainActivity: No network connection!
```
**原因**：设备没有网络连接
**解决**：
- 模拟器：确保模拟器可以上网
- 真机：检查 WiFi 或移动数据

#### C. 请求日志

应该看到：
```
D/MainActivity: Request URL: https://iot-api.heclouds.com/thingmodel/query-device-property?product_id=3ffd453bafa04ffbb8775ee13eead513&device_name=test1
D/MainActivity: Authorization: version=2020-05-29&res=...
```

#### D. 响应日志

**成功情况**：
```
D/MainActivity: Response Code: 200
D/MainActivity: Response Body: {"errno":0,"data":[...]}
```

**失败情况 1 - 鉴权失败**：
```
D/MainActivity: Response Code: 401
E/MainActivity: HTTP Error: 401
E/MainActivity: Error Response: {"errno":401,"error":"authentication failed"}
```

**失败情况 2 - 参数错误**：
```
D/MainActivity: Response Code: 200
D/MainActivity: Response Body: {"errno":4,"error":"invalid parameter"}
E/MainActivity: OneNET Error: errno=4, errmsg=invalid parameter
```

**失败情况 3 - 设备不存在**：
```
D/MainActivity: Response Code: 200
D/MainActivity: Response Body: {"errno":7,"error":"device not found"}
E/MainActivity: OneNET Error: errno=7, errmsg=device not found
```

---

## 🛠️ 常见问题及解决方案

### 问题 1: Token 生成失败

**症状**：
- Logcat 显示：`ERROR: Token is empty!`
- 或者：`Token generation failed: ...`

**可能原因**：
1. `user_accesskey` 不是有效的 Base64 字符串
2. AccessKey 格式不正确

**解决方法**：

OneNET 的 AccessKey 通常是一个较长的 Base64 字符串。您提供的 `67k36rzgOO` 看起来太短了。

**请确认**：
1. 登录 OneNET 控制台
2. 进入产品管理 → 找到您的产品
3. 查看"API密钥"或"鉴权信息"
4. 复制完整的 AccessKey

正确的 AccessKey 应该类似：
```
HFoS226mWLNYT3IYJl3aCrsTAeGaSFfOPm9U2k4NKfx6wqTXyhSI6b2L0O67PPxHA08ZVWeqxCb/cTCgtoOBrw==
```

**修改代码**：
```java
private String user_accesskey="YOUR_FULL_ACCESS_KEY_HERE";
```

### 问题 2: HTTP 401 鉴权失败

**症状**：
```
Response Code: 401
Error Response: {"errno":401,"error":"authentication failed"}
```

**可能原因**：
1. Token 生成错误
2. AccessKey 不正确
3. userID 不正确

**解决方法**：
1. 确认 AccessKey 是否正确（见问题 1）
2. 确认 userID 是否正确（应该是用户 ID，不是产品 ID）
3. 检查 token.java 中的签名算法是否正确

**注意**：在 OneNET 中：
- `user_id` 应该是**用户 ID**，不是产品 ID
- 但有些情况下，产品 ID 也可以作为 user_id 使用

尝试修改：
```java
// 如果当前是产品 ID，尝试改为真正的用户 ID
private String user_id="YOUR_USER_ID"; // 从 OneNET 控制台获取
```

### 问题 3: 无网络连接

**症状**：
```
E/MainActivity: No network connection!
Toast: 无网络连接
```

**解决方法**：

**模拟器**：
1. 确保模拟器可以访问互联网
2. 尝试在模拟器的浏览器中打开网页
3. 如果不能上网，重启模拟器或检查代理设置

**真机**：
1. 检查 WiFi 或移动数据是否开启
2. 确保应用有网络权限（已在 AndroidManifest.xml 中添加）

### 问题 4: 设备不存在或没有数据

**症状**：
```
Response Code: 200
Response Body: {"errno":0,"data":[]}
E/MainActivity: data is null, response: {...}
```

**原因**：
- 设备存在，但没有上报 humidity 和 light 数据

**解决方法**：
1. 登录 OneNET 控制台
2. 进入设备管理 → test1
3. 查看"物模型数据"
4. 确认是否有 humidity 和 light 的数据记录
5. 如果没有，需要在设备上运行程序上报数据

### 问题 5: 参数错误

**症状**：
```
Response Body: {"errno":4,"error":"invalid parameter"}
```

**可能原因**：
1. product_id 不正确
2. device_name 不正确
3. URL 格式错误

**解决方法**：
1. 登录 OneNET 控制台
2. 确认产品 ID：`3ffd453bafa04ffbb8775ee13eead513`
3. 确认设备名称：`test1`（区分大小写）
4. 在浏览器中测试 URL：
   ```
   https://iot-api.heclouds.com/thingmodel/query-device-property?product_id=3ffd453bafa04ffbb8775ee13eead513&device_name=test1
   ```

---

## 🧪 手动测试 API

### 方法 1: 使用 cURL（推荐）

在电脑命令行中执行：

```bash
# 先获取 Token（需要手动计算或使用工具）
# 然后测试 API
curl -X GET "https://iot-api.heclouds.com/thingmodel/query-device-property?product_id=3ffd453bafa04ffbb8775ee13eead513&device_name=test1" \
  -H "authorization: YOUR_TOKEN_HERE"
```

### 方法 2: 使用 Postman

1. 打开 Postman
2. 创建 GET 请求
3. URL: `https://iot-api.heclouds.com/thingmodel/query-device-property?product_id=3ffd453bafa04ffbb8775ee13eead513&device_name=test1`
4. Headers 添加：
   - Key: `authorization`
   - Value: `YOUR_TOKEN_HERE`
5. 点击 Send

### 方法 3: 使用浏览器（仅测试 URL）

在浏览器中打开：
```
https://iot-api.heclouds.com/thingmodel/query-device-property?product_id=3ffd453bafa04ffbb8775ee13eead513&device_name=test1
```

应该会返回错误（因为没有 authorization），但可以确认 URL 是否正确。

---

## 📝 最可能的问题

根据经验，**最可能的原因是 AccessKey 不正确**。

### 验证 AccessKey

您提供的 AccessKey 是：`67k36rzgOO`

这个字符串：
- 长度只有 10 个字符
- 不是标准的 Base64 格式（Base64 长度应该是 4 的倍数）

**正常的 OneNET AccessKey 应该类似**：
```
HFoS226mWLNYT3IYJl3aCrsTAeGaSFfOPm9U2k4NKfx6wqTXyhSI6b2L0O67PPxHA08ZVWeqxCb/cTCgtoOBrw==
```

### 如何获取正确的 AccessKey

1. 登录 [OneNET 控制台](https://open.iot.10086.cn/)
2. 进入"开发者中心"
3. 找到您的产品
4. 点击"API密钥"或"鉴权信息"
5. 复制完整的 AccessKey（通常很长）
6. 替换代码中的 `user_accesskey`

---

## 🎯 快速诊断清单

请按顺序检查：

- [ ] 1. Logcat 中是否看到 "Generated Token:" 日志？
- [ ] 2. Token 是否为空？
- [ ] 3. 是否看到 "Network connected:" 日志？
- [ ] 4. 是否看到 "Request URL:" 日志？
- [ ] 5. Response Code 是多少？（200、401、403...）
- [ ] 6. Response Body 是什么内容？
- [ ] 7. 是否有 errno 错误码？
- [ ] 8. OneNET 控制台中设备是否在线？
- [ ] 9. 设备是否上报了 humidity 和 light 数据？

---

## 📞 下一步

**请提供以下信息**：

1. **Logcat 完整日志**（从启动到第一次刷新）
2. **Response Code** 是多少？
3. **Response Body** 是什么内容？
4. **AccessKey 的长度**是多少？（数一下字符数）
5. **OneNET 控制台截图**（设备列表和物模型数据）

有了这些信息，我可以准确判断问题所在并提供解决方案。

---

## 💡 临时调试建议

如果您想快速测试，可以尝试：

1. **在 Logcat 中搜索 "Authorization:"**
   - 查看生成的 Token 是什么样的
   - 复制这个 Token

2. **使用 Postman 或 cURL 测试**
   - 用复制的 Token 测试 API
   - 看是否能获取数据

3. **如果 Postman 也失败**
   - 说明 Token 或参数有问题
   - 检查 OneNET 控制台的配置

4. **如果 Postman 成功**
   - 说明 API 配置正确
   - 问题可能在 Android 代码的其他地方

---

**请提供 Logcat 日志，我会帮您进一步分析！** 🔍
