# MQTT改造快速测试指南

## 编译和运行

### 1. 同步Gradle依赖
在Android Studio中：
- 点击 "Sync Project with Gradle Files" 按钮
- 等待MQTT依赖下载完成

### 2. 编译项目
- 点击 "Build" → "Make Project"
- 确保没有编译错误

### 3. 运行APP
- 连接Android设备或启动模拟器
- 点击 "Run" 按钮

## 测试步骤

### 测试1：MQTT连接测试

**预期结果：**
- APP启动后，右上角显示"已连接"（绿色）
- Toast提示"MQTT连接成功"

**如果失败：**
- 检查网络连接
- 检查PRODUCT_ID、DEVICE_NAME、ACCESS_KEY配置
- 查看Logcat日志，过滤"MqttManager"标签

---

### 测试2：初始状态测试

**操作步骤：**
1. 刚打开APP，未收到任何数据

**预期结果：**
- 温度显示："--"
- 湿度显示："--"
- 光照显示："--"
- 最后更新显示："最后更新：--"
- 告警状态显示："正常"（绿色背景）

---

### 测试3：环境数据接收测试

**操作步骤：**
1. 设备上报温度数据（例如：25.5°C）
2. 设备上报湿度数据（例如：60%RH）
3. 设备上报光照数据（例如：500Lux）

**预期结果：**
- 温度卡片显示："25.5"
- 湿度卡片显示："60"
- 光照卡片显示："500"
- 最后更新时间更新为当前时间
- 数据保持显示，不会清空

**验证点：**
- ✅ 数据收到后立即显示
- ✅ 数据不会变成0或清空
- ✅ 最后更新时间正确更新

---

### 测试4：告警状态实时测试

**操作步骤：**
1. 设备上报 alarm_state = 1（告警触发）

**预期结果：**
- 告警状态卡片变为红色背景
- 文字显示："告警触发"（红色）
- 告警详情卡片变为红色背景
- 文字显示："当前状态：倾斜告警"（红色）

**操作步骤：**
2. 设备上报 alarm_state = 0（告警解除）

**预期结果：**
- 告警状态卡片变为绿色背景
- 文字显示："正常"（绿色）
- 告警详情卡片变为绿色背景
- 文字显示："当前状态：正常"（绿色）

**验证点：**
- ✅ 告警状态立即切换
- ✅ 颜色变化符合规范（绿/红）
- ✅ 两个告警卡片同步更新

---

### 测试5：离线检测测试

**操作步骤：**
1. 让设备停止上报数据
2. 等待30秒以上

**预期结果：**
- 右上角显示："未连接"（红色）
- 所有控制按钮置灰（半透明）
- 控制按钮无法点击
- Logcat显示警告："设备超时30秒无数据，判定为离线"

**验证点：**
- ✅ 30秒后判定离线（不是立即）
- ✅ 离线后按钮禁用
- ✅ 已有数据保持显示，不清空

---

### 测试6：控制指令测试

**前提条件：**
- 设备处于在线状态

**操作步骤：**
1. 点击"LED 开"按钮

**预期结果：**
- LED开按钮立即变为绿色
- LED关按钮变为灰色
- Toast提示："LED 开启"
- 设备收到指令后上报led状态
- APP接收上报后确认状态

**操作步骤：**
2. 点击"蜂鸣器 关"按钮

**预期结果：**
- 蜂鸣器关按钮立即变为红色
- 蜂鸣器开按钮变为灰色
- Toast提示："蜂鸣器 关闭"

**验证点：**
- ✅ 本地优先更新（点击后立即变化）
- ✅ 通过MQTT发送指令
- ✅ 最终状态以上报为准

---

### 测试7：离线控制测试

**前提条件：**
- 设备处于离线状态（30秒无数据）

**操作步骤：**
1. 尝试点击任意控制按钮

**预期结果：**
- 按钮无响应（禁用状态）
- Toast提示："设备未连接，无法控制"

---

### 测试8：数据持久性测试

**操作步骤：**
1. 等待设备上报一次完整数据
2. 断开设备网络或停止上报
3. 观察APP界面

**预期结果：**
- 温度、湿度、光照数据保持最后值
- 不会显示0或清空
- 最后更新时间保持不变
- 30秒后显示离线，但数据仍在

**验证点：**
- ✅ 数据不清空
- ✅ 不显示0
- ✅ 保持最后一次有效值

---

### 测试9：刷新按钮测试

**操作步骤：**
1. 点击"刷新数据"按钮

**预期结果：**
- Toast提示："MQTT被动接收数据，无需手动刷新"
- 显示最后更新时间

**验证点：**
- ✅ 不做主动刷新
- ✅ 提示用户MQTT是被动接收

---

## Logcat日志检查

### 关键日志标签

1. **MqttManager** - MQTT连接和消息日志
   ```
   D/MqttManager: 正在连接MQTT服务器...
   D/MqttManager: MQTT连接成功
   D/MqttManager: 订阅主题成功: $sys/67k36rzgOO/test1/property/post/reply
   D/MqttManager: 收到MQTT消息 - Topic: ..., Payload: ...
   ```

2. **MainActivity** - 数据处理日志
   ```
   D/MainActivity: 收到MQTT消息: {...}
   D/MainActivity: 更新温度: 25.5
   D/MainActivity: 更新湿度: 60
   D/MainActivity: 更新光照: 500
   D/MainActivity: 告警状态: false
   D/MainActivity: LED状态: true
   W/MainActivity: 设备超时30秒无数据，判定为离线
   ```

### 常见错误日志

1. **MQTT连接失败**
   ```
   E/MqttManager: MQTT连接失败
   ```
   - 检查网络
   - 检查配置参数

2. **JSON解析失败**
   ```
   E/MainActivity: 解析MQTT消息失败
   ```
   - 检查数据格式
   - 确认OneNET返回格式正确

---

## 问题排查

### 问题1：MQTT连接失败

**可能原因：**
- 网络不通
- PRODUCT_ID或DEVICE_NAME错误
- ACCESS_KEY无效或过期

**解决方法：**
1. 检查设备网络连接
2. 确认MqttManager.java中的配置：
   ```java
   private static final String PRODUCT_ID = "67k36rzgOO";
   private static final String DEVICE_NAME = "test1";
   private static final String ACCESS_KEY = "3ffd453bafa04ffbb8775ee13eead513";
   ```

### 问题2：收不到数据

**可能原因：**
- 设备未上报数据
- 主题订阅错误
- 数据格式不匹配

**解决方法：**
1. 确认设备正在上报数据
2. 检查OneNET平台设备状态
3. 查看Logcat是否有MQTT消息到达

### 问题3：数据显示不正确

**可能原因：**
- JSON解析错误
- identifier名称不匹配

**解决方法：**
1. 检查DealJsonDataFromMqtt方法中的identifier判断
2. 确认设备上报的字段名与代码一致

### 问题4：离线检测不准确

**可能原因：**
- lastDataReceiveTime未正确更新
- 定时器未启动

**解决方法：**
1. 检查DealJsonDataFromMqtt中是否更新了lastDataReceiveTime
2. 确认startOfflineDetection在onCreate中被调用

---

## 性能监控

### 资源占用检查

1. **CPU使用率**
   - MQTT方式应该比HTTP轮询更低
   - 无明显CPU峰值

2. **网络流量**
   - 只在有数据时产生流量
   - 无周期性请求流量

3. **电量消耗**
   - 比HTTP轮询更省电
   - 无频繁网络请求

---

## 验收标准

所有测试项通过后，确认以下验收标准：

- [x] MQTT连接正常
- [x] 环境数据正确显示
- [x] 告警状态实时切换
- [x] 离线检测准确（30秒）
- [x] 控制指令正常发送
- [x] 数据持久性良好
- [x] 无内存泄漏
- [x] 无崩溃现象
- [x] UI流畅无卡顿
- [x] 符合所有开发规范

---

## 下一步

测试通过后：
1. 在实际设备上验证
2. 长时间运行稳定性测试
3. 不同网络环境测试（WiFi/4G/5G）
4. 边界条件测试（网络切换、后台运行等）

祝测试顺利！🎉
