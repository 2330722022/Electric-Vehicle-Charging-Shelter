# OneNET 物联网监控 APP 迭代升级说明

## 修改概述
本次升级在保留原有代码结构和逻辑的基础上，对 APP 进行了功能增强和 UI 优化，实现了完整的传感器数据监控和设备控制功能。

---

## 一、布局文件修改 (activity_main.xml)

### 1. 新增温度数据卡片
- **位置**: 第 96-138 行
- **内容**: 添加了温度显示卡片，与湿度、光照卡片样式统一
- **字段标识符**: `temperature`（严格匹配 OneNET 物模型）

### 2. 新增告警状态卡片
- **位置**: 第 59-94 行
- **ID**: `cardAlarm`
- **功能**: 
  - 正常状态显示绿色背景 (#4CAF50)
  - 告警触发时显示红色背景 (#F44336)
  - 动态更新告警文本

### 3. 新增振动和倾斜角卡片
- **位置**: 第 230-310 行
- **布局**: 采用横向双卡片布局，节省空间
- **字段标识符**: 
  - `vibration` - 振动状态
  - `tilt_angle` - 倾斜角度

### 4. 新增风扇控制按钮
- **位置**: 第 378-406 行
- **按钮 ID**: 
  - `btnFanOn` - 风扇开启
  - `btnFanOff` - 风扇关闭
- **字段标识符**: `fan_en`

### 5. UI 样式优化
- **卡片圆角**: 统一为 12dp（原 8dp）
- **卡片阴影**: 统一为 3dp（原 4dp）
- **卡片间距**: 统一为 12dp（原 16dp）
- **内边距**: 统一为 16dp（原 20dp）
- **字体大小**: 
  - 标题: 14sp（原 16sp）
  - 数值: 28sp（原 32sp）
  - 单位: 13sp（原 14sp）

---

## 二、Java 代码修改 (MainActivity.java)

### 1. 新增导入包
```java
// 第 6 行
import android.view.View;
// 第 12 行
import androidx.cardview.widget.CardView;
// 第 27-29 行
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Locale;
```

### 2. 新增成员变量（第 41-51 行）
```java
// 传感器数据变量
String humidity_value, light_value, temperature_value, vibration_value, tilt_angle_value;

// 设备状态变量
boolean alarm_state = false;      // 告警状态
boolean led_state = false;        // LED 状态
boolean beep_state = false;       // 蜂鸣器状态
boolean fan_state = false;        // 风扇状态
boolean isDeviceOnline = false;   // 设备在线状态

// UI 控件引用
private TextView humidity_tv, light_tv, temperature_tv, vibration_tv, tilt_angle_tv;
private TextView connection_status_tv, last_update_tv, alarm_state_tv;
private CardView card_alarm;
private Button btnBeepOn, btnBeepOff, btnLedOn, btnLedOff, btnFanOn, btnFanOff;
```

### 3. 新增 updateUI() 方法（第 109-153 行）
**功能**:
- 统一更新所有传感器数据显示
- 无数据时显示 "--"
- 根据告警状态动态改变卡片颜色
- 更新连接状态显示
- 自动刷新最后更新时间（格式: yyyy-MM-dd HH:mm:ss）
- 离线时禁用所有控制按钮
- 更新按钮高亮状态

### 4. 新增 setControlButtonsEnabled() 方法（第 155-174 行）
**功能**:
- 设备离线时禁用所有控制按钮
- 通过设置透明度（0.5f）区分可用/不可用状态

### 5. 新增 updateButtonStates() 方法（第 176-207 行）
**功能**:
- LED 按钮：开启时绿色高亮，关闭时灰色
- 蜂鸣器按钮：开启时橙色高亮，关闭时灰色
- 风扇按钮：开启时青色高亮，关闭时灰色
- 实现互斥视觉效果

### 6. 修改 onCreate() 方法
**新增控件初始化**（第 78-86 行）:
```java
temperature_tv = findViewById(R.id.tvTemperature);
vibration_tv = findViewById(R.id.tvVibration);
tilt_angle_tv = findViewById(R.id.tvTiltAngle);
last_update_tv = findViewById(R.id.tvLastUpdate);
alarm_state_tv = findViewById(R.id.tvAlarmState);
card_alarm = findViewById(R.id.cardAlarm);
btnFanOn = findViewById(R.id.btnFanOn);
btnFanOff = findViewById(R.id.btnFanOff);
```

**新增风扇控制事件**（第 101-102 行）:
```java
btnFanOn.setOnClickListener(v -> controlDevice("fan_en", true));
btnFanOff.setOnClickListener(v -> controlDevice("fan_en", false));
```

**新增手动刷新按钮**（第 104-110 行）:
```java
Button btnRefresh = findViewById(R.id.btnRefresh);
btnRefresh.setOnClickListener(v -> {
    Toast.makeText(MainActivity.this, "正在刷新数据...", Toast.LENGTH_SHORT).show();
    GetOnenetData();
    updateUI();
});
```

### 7. 修改 task Runnable（第 53-62 行）
**修改前**:
```java
humidity_tv.setText(humidity_value);
light_tv.setText(light_value);
```

**修改后**:
```java
updateUI();  // 统一调用 UI 更新方法
```

### 8. 修改 GetOnenetData() 方法
**连接状态管理**（第 219 行）:
```java
isDeviceOnline = true;  // 成功获取数据时标记在线
```

**异常处理**（第 274-289 行）:
```java
// 所有异常捕获中都添加
isDeviceOnline = false;  // 发生异常时标记离线
```

### 9. 重写 DealJsonData() 方法（第 395-447 行）
**完整解析所有物模型字段**:
```java
// 温度
if(identifier.equals("temperature")) {
    temperature_value = val;
}
// 湿度
else if(identifier.equals("humidity")) {
    humidity_value = val;
}
// 光照
else if(identifier.equals("light")) {
    light_value = val;
}
// 振动
else if(identifier.equals("vibration")) {
    vibration_value = val;
}
// 倾斜角
else if(identifier.equals("tilt_angle")) {
    tilt_angle_value = val;
}
// 告警状态（支持 "0"/"1" 和 "true"/"false"）
else if(identifier.equals("alarm_state")) {
    alarm_state = val.equals("1") || val.equalsIgnoreCase("true");
}
// LED 状态
else if(identifier.equals("led")) {
    led_state = val.equals("1") || val.equalsIgnoreCase("true");
}
// 蜂鸣器状态
else if(identifier.equals("beep")) {
    beep_state = val.equals("1") || val.equalsIgnoreCase("true");
}
// 风扇状态
else if(identifier.equals("fan_en")) {
    fan_state = val.equals("1") || val.equalsIgnoreCase("true");
}
```

**错误处理**:
- API 返回错误码时设置 `isDeviceOnline = false`
- data 为 null 时设置 `isDeviceOnline = false`
- 成功解析后设置 `isDeviceOnline = true`

### 10. 修改 controlDevice() 方法
**增强 Toast 反馈**（第 330-353 行）:
```java
// 更新本地状态
if (property.equals("led")) {
    led_state = value;
} else if (property.equals("beep")) {
    beep_state = value;
} else if (property.equals("fan_en")) {
    fan_state = value;
}

// 更新按钮状态
updateButtonStates();

// 中文设备名称映射
String deviceName = "";
if (property.equals("led")) {
    deviceName = "LED";
} else if (property.equals("beep")) {
    deviceName = "蜂鸣器";
} else if (property.equals("fan_en")) {
    deviceName = "风扇";
}

Toast.makeText(MainActivity.this, 
    deviceName + (value ? " 开启成功" : " 关闭成功"), 
    Toast.LENGTH_SHORT).show();
```

---

## 三、功能特性总结

### 1. 传感器数据监控
✅ 温度 (temperature)  
✅ 湿度 (humidity)  
✅ 光照 (light)  
✅ 振动 (vibration)  
✅ 倾斜角 (tilt_angle)  

### 2. 设备控制
✅ LED 开关 (led)  
✅ 蜂鸣器开关 (beep)  
✅ 风扇开关 (fan_en)  

### 3. 告警管理
✅ 告警状态实时监控 (alarm_state)  
✅ 告警触发时界面红色提醒  
✅ 正常状态绿色显示  

### 4. 交互优化
✅ 控制按钮状态高亮（开启/关闭不同颜色）  
✅ 按钮互斥效果（同一设备的开/关按钮）  
✅ 操作 Toast 反馈（中文提示）  
✅ 设备离线时按钮置灰不可点击  
✅ 手动刷新数据功能  

### 5. UI 优化
✅ 统一卡片圆角（12dp）  
✅ 统一卡片阴影（3dp）  
✅ 统一间距（12dp）  
✅ 统一字体层级  
✅ 布局自适应手机屏幕  

### 6. 数据展示
✅ 无数据/离线时显示 "--"  
✅ 自动刷新最后更新时间（yyyy-MM-dd HH:mm:ss）  
✅ 连接状态实时显示（已连接/离线）  

---

## 四、OneNET 物模型字段对照表

| 字段标识符 | 中文名称 | 数据类型 | 说明 |
|-----------|---------|---------|------|
| temperature | 温度 | String | 温度值，单位 °C |
| humidity | 湿度 | String | 湿度值，单位 % RH |
| light | 光照 | String | 光照强度，单位 Lux |
| vibration | 振动 | String | 振动状态 |
| tilt_angle | 倾斜角 | String | 倾斜角度，单位 ° |
| alarm_state | 告警状态 | Boolean | 0/false=正常，1/true=告警 |
| led | LED | Boolean | 0/false=关闭，1/true=开启 |
| beep | 蜂鸣器 | Boolean | 0/false=关闭，1/true=开启 |
| fan_en | 风扇 | Boolean | 0/false=关闭，1/true=开启 |

---

## 五、编译和运行

### 编译要求
- Android Studio 版本: 任意最新版本
- 最低 SDK: API 26 (Android 8.0)
- 目标 SDK: API 36
- Java 版本: Java 11

### 编译步骤
1. 打开 Android Studio
2. 选择 File → Open，打开项目文件夹
3. 等待 Gradle 同步完成
4. 点击 Build → Make Project
5. 确认无编译错误

### 运行测试
1. 连接 Android 设备或启动模拟器
2. 点击 Run → Run 'app'
3. 确保设备已接入网络
4. 观察传感器数据是否实时刷新
5. 测试控制按钮功能

---

## 六、注意事项

### 1. Token 配置
当前使用固定 Token，如需动态生成，请取消注释 token.java 相关代码。

### 2. 设备信息
- 产品 ID: `67k36rzgOO`
- 设备名称: `test1`
- AccessKey: `3ffd453bafa04ffbb8775ee13eead513`

如需修改，请同时更新：
- MainActivity.java 第 32-36 行
- token.java 第 74 行

### 3. 网络权限
确保 AndroidManifest.xml 中包含网络权限：
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

### 4. 数据刷新间隔
当前设置为 2 秒自动刷新，可在 MainActivity.java 第 55 行修改：
```java
handler.postDelayed(this, 2000); // 修改 2000 为所需毫秒数
```

---

## 七、主要修改文件清单

1. **app/src/main/res/layout/activity_main.xml**
   - 新增温度、振动、倾斜角卡片
   - 新增告警状态卡片
   - 新增风扇控制按钮
   - 优化整体 UI 样式

2. **app/src/main/java/com/example/onenet215/MainActivity.java**
   - 新增传感器数据解析逻辑
   - 新增告警状态处理
   - 新增 UI 更新方法
   - 新增按钮状态管理
   - 完善控制反馈机制

3. **无需修改的文件**
   - token.java（保持不变）
   - build.gradle.kts（保持不变）
   - AndroidManifest.xml（保持不变）

---

## 八、联调建议

### 固件端
1. 确保固件正确上报所有物模型字段
2. 字段标识符必须与 APP 严格一致
3. 数据类型建议使用字符串格式

### 云端配置
1. 在 OneNET 平台创建对应的物模型
2. 确保设备已正确绑定到产品
3. 验证 API 访问权限

### APP 端
1. 首次运行检查连接状态
2. 观察数据刷新是否正常
3. 测试远程控制指令下发
4. 验证告警状态变化

---

## 九、常见问题排查

### 1. 显示"离线"状态
- 检查网络连接
- 验证产品 ID 和设备名称是否正确
- 检查 Token 是否有效
- 查看 Logcat 日志中的错误信息

### 2. 数据显示 "--"
- 确认云端有对应字段的数据
- 检查字段标识符是否匹配
- 查看 API 返回的 JSON 数据结构

### 3. 控制按钮无响应
- 检查设备是否在线
- 验证 controlDevice() 方法是否正常执行
- 查看 Logcat 中的控制请求日志

### 4. 告警状态不变化
- 确认云端 alarm_state 字段是否正确更新
- 检查字段值是否为 "0"/"1" 或 "true"/"false"

---

## 十、后续优化建议

1. **增加下拉刷新**: 使用 SwipeRefreshLayout 实现下拉刷新
2. **增加图表展示**: 集成 MPAndroidChart 显示历史数据趋势
3. **本地缓存**: 使用 Room 数据库缓存历史数据
4. **推送通知**: 告警触发时发送系统通知
5. **多设备支持**: 支持切换管理多个设备
6. **主题切换**: 支持深色/浅色主题切换

---

**修改完成日期**: 2026-05-11  
**版本**: v2.0  
**兼容性**: 完全兼容原有 v1.0 版本功能
