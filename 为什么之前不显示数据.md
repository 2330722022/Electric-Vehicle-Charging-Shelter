# 为什么之前不显示数据？- 根本原因分析

## 🔴 问题根源

### 之前的错误做法 ❌

```java
// 错误：直接使用 accesskey 作为 authorization
connection.setRequestProperty("authorization", "67k36rzgOO");
```

**结果**: 
- HTTP 401 鉴权失败
- 或者返回空数据

### 正确的做法（老师代码）✅

```java
// 1. 使用 token.java 生成完整的 Token
token tk = new token();
String ok_key = tk.key(user_id, user_accesskey);
// 生成结果: "version=2020-05-29&res=userid%2F3ffd453bafa04ffbb8775ee13eead513&et=1956499200&method=sha1&sign=xxxxx"

// 2. 使用生成的 Token 作为 authorization
connection.setRequestProperty("authorization", ok_key);
```

**结果**: 
- HTTP 200 成功
- 返回正确的 JSON 数据

---

## 📝 OneNET API 鉴权方式说明

OneNET 平台支持多种鉴权方式：

### 方式 1: Token 鉴权（推荐，老师使用的方式）

**适用场景**: 
- 物模型查询接口
- 物模型设置接口
- 大多数新版 API

**Token 格式**:
```
version=2020-05-29&res=userid/{user_id}&et={expiration_time}&method={signature_method}&sign={signature}
```

**生成步骤**:
1. 构造签名字符串：`expirationTime + "\n" + signatureMethod + "\n" + resourceName + "\n" + version`
2. 使用 HMAC-SHA1 加密（密钥是 Base64 解码后的 accesskey）
3. Base64 编码签名结果
4. URL 编码所有参数
5. 组装成 Token 字符串

**请求头**:
```
authorization: version=2020-05-29&res=userid%2F...&et=1956499200&method=sha1&sign=xxx
```

### 方式 2: API-Key 鉴权

**适用场景**:
- 旧版 API
- 某些特定接口

**请求头**:
```
api-key: YOUR_API_KEY
```

### 方式 3: Token Bearer 鉴权

**适用场景**:
- OAuth 2.0 流程

**请求头**:
```
authorization: Bearer YOUR_TOKEN
```

---

## 🔍 为什么老师的代码要用 Token？

### 原因 1: 安全性更高

- API-Key 是明文传输，容易被截获
- Token 包含签名，可以验证完整性
- Token 有过期时间（et 字段），更安全

### 原因 2: OneNET 新版 API 要求

- 物模型接口（thingmodel）要求使用 Token 鉴权
- 简单的 api-key 可能不被支持

### 原因 3: 细粒度权限控制

- Token 可以指定资源（res 字段）
- 可以设置不同的过期时间
- 可以限制访问范围

---

## 📊 Token 生成过程详解

### 输入参数

```java
userID = "3ffd453bafa04ffbb8775ee13eead513"
UserAccessKey = "67k36rzgOO"
version = "2020-05-29"
resourceName = "userid/3ffd453bafa04ffbb8775ee13eead513"
expirationTime = "1956499200"
signatureMethod = "sha1"
```

### 步骤 1: 构造签名字符串

```
encryptText = "1956499200\nsha1\nuserid/3ffd453bafa04ffbb8775ee13eead513\n2020-05-29"
```

### 步骤 2: HMAC-SHA1 加密

```java
// 1. Base64 解码 accesskey
byte[] keyBytes = Base64.getDecoder().decode("67k36rzgOO");

// 2. 创建 HmacSHA1 密钥
SecretKeySpec key = new SecretKeySpec(keyBytes, "HmacSHA1");

// 3. 创建 Mac 对象
Mac mac = Mac.getInstance("HmacSHA1");
mac.init(key);

// 4. 计算签名
byte[] signatureBytes = mac.doFinal(encryptText.getBytes());
```

### 步骤 3: Base64 编码签名

```java
String signature = Base64.getEncoder().encodeToString(signatureBytes);
// 例如: "abc123xyz..."
```

### 步骤 4: URL 编码

```java
String res = URLEncoder.encode("userid/3ffd453bafa04ffbb8775ee13eead513", "UTF-8");
// 结果: "userid%2F3ffd453bafa04ffbb8775ee13eead513"

String sig = URLEncoder.encode(signature, "UTF-8");
```

### 步骤 5: 组装 Token

```
version=2020-05-29
&res=userid%2F3ffd453bafa04ffbb8775ee13eead513
&et=1956499200
&method=sha1
&sign=abc123xyz...
```

---

## 🎯 关键区别总结

| 项目 | 之前的错误做法 | 现在的正确做法 |
|------|--------------|--------------|
| 鉴权方式 | 直接使用 accesskey | 使用 Token 鉴权 |
| 请求头 | `authorization: 67k36rzgOO` | `authorization: version=...&res=...&sign=...` |
| Token 生成 | 无 | 使用 token.java 生成 |
| 加密算法 | 无 | HMAC-SHA1 |
| 安全性 | 低 | 高 |
| 是否符合 OneNET 规范 | ❌ | ✅ |

---

## 💡 如何验证 Token 是否正确？

### 方法 1: 查看日志

运行应用后，在 Logcat 中搜索 `System.out`，应该看到：

```
I/System.out: Authorization:version=2020-05-29&res=userid%2F3ffd453bafa04ffbb8775ee13eead513&et=1956499200&method=sha1&sign=xxxxx
```

如果看到这行日志，说明 Token 生成成功。

### 方法 2: 使用 cURL 测试

在电脑上测试（需要先手动生成 Token）：

```bash
curl -X GET "https://iot-api.heclouds.com/thingmodel/query-device-property?product_id=3ffd453bafa04ffbb8775ee13eead513&device_name=test1" \
  -H "authorization: version=2020-05-29&res=userid%2F3ffd453bafa04ffbb8775ee13eead513&et=1956499200&method=sha1&sign=xxxxx"
```

如果返回 JSON 数据，说明 Token 正确。

### 方法 3: 查看 HTTP 响应码

- **200**: Token 正确，鉴权成功
- **401**: Token 错误，鉴权失败
- **403**: Token 有效，但无权访问该资源

---

## 🚨 常见错误

### 错误 1: AccessKey 不是 Base64 格式

**现象**: 
```
InvalidKeyException: Key length must be multiple of 4
```

**原因**: 
- `67k36rzgOO` 看起来不像标准的 Base64 字符串
- 正常的 Base64 字符串长度应该是 4 的倍数

**解决**: 
- 确认 OneNET 控制台显示的 AccessKey 是否正确
- 可能需要使用完整的 AccessKey（可能更长）

### 错误 2: userID 和 product_id 混淆

**现象**: 
- Token 生成成功
- 但返回 403 或空数据

**原因**: 
- userID 应该是用户 ID，不是产品 ID
- 但在您的情况下，两者可能相同

**解决**: 
- 登录 OneNET 控制台
- 查看"账号信息"中的用户 ID
- 确认是否和产品 ID 相同

### 错误 3: 设备名称错误

**现象**: 
- 鉴权成功（HTTP 200）
- 但 data 数组为空

**原因**: 
- 设备名称拼写错误
- 设备不存在

**解决**: 
- 确认设备名称是 `test1`（区分大小写）
- 在 OneNET 控制台确认设备存在

---

## ✅ 最终验证步骤

1. **运行应用**
2. **查看 Logcat**，确认看到 `Authorization:` 日志
3. **等待 2-3 秒**，让自动刷新执行
4. **观察界面**，湿度和光照应该显示数值
5. **如果还是 "--"**，检查：
   - OneNET 控制台设备是否在线
   - 设备是否上报了 humidity 和 light 数据
   - 物模型标识符是否正确

---

## 📞 如果还有问题

请提供以下信息：

1. **Logcat 完整日志**（从启动到第一次刷新）
2. **OneNET 控制台截图**（设备列表和物模型数据）
3. **HTTP 响应码**（200、401、403 等）
4. **Token 生成日志**（System.out 输出的 Authorization）

---

**这就是之前不显示数据的根本原因：没有使用正确的 Token 鉴权方式！** 🎯
