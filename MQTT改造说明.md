# MQTT改造说明文档

## 改造概述

本次改造将APP从HTTP轮询方式升级为MQTT被动接收方式，完全遵循设备上传规则和APP开发规范。

## 设备上传规则实现

### 1. 环境数据（温度/湿度/光照）
- ✅ **非实时，定时隔一段时间上报一次**
- ✅ APP端收到一次更新一次界面
- ✅ 未收到时保持最后一次值，不清空、不显示0
- ✅ 刚打开APP未收到数据时，数值显示"--"

### 2. 告警数据（alarm_state/振动/倾角）
- ✅ **实时触发，立即上传**
- ✅ APP端实时监听，收到后立即刷新界面
- ✅ 切换颜色：正常显示绿色，告警显示红色

### 3. 设备状态（led/beep/舵机）
- ✅ **变化时上传**
- ✅ APP端接收状态更新并同步显示

## APP端开发规则实现

### 1. MQTT只订阅不轮询
- ✅ 使用Eclipse Paho MQTT客户端
- ✅ 被动接收数据，不主动请求云端
- ✅ 删除了所有HTTP轮询代码（GetOnenetData方法已废弃）

### 2. 环境数据处理
- ✅ 收到一次更新一次界面
- ✅ 未收到时保持最后一次值
- ✅ 不清空、不显示0
- ✅ 初始状态显示"--"

### 3. 告警状态实时监听
- ✅ 收到后立即刷新界面
- ✅ 切换颜色：
  - 正常：绿色背景 (#C8E6C9)，绿色文字 (#2E7D32)
  - 告警：红色背景 (#FFCDD2)，红色文字 (#C62828)

### 4. 最后更新时间
- ✅ 只在收到新数据时更新
- ✅ 未收到数据显示"--"

### 5. 离线检测机制
- ✅ 设备长时间未上报任何数据（30秒以上）判定离线
- ✅ 每5秒检查一次最后接收时间
- ✅ 离线后按钮置灰（alpha=0.5）
- ✅ 禁用所有控制按钮

### 6. 初始状态处理
- ✅ 刚打开APP未收到数据时，数值显示"--"
- ✅ 收到一次后保持最新值

### 7. 不做定时自动刷新
- ✅ 删除了2秒定时轮询任务
- ✅ 不做无效UI刷新
- ✅ 节省手机资源

### 8. 控制指令下发
- ✅ 本地优先更新界面提升体验
- ✅ 通过MQTT发布控制指令
- ✅ 最终以上报状态为准

### 9. 界面风格
- ✅ 保持原版风格
- ✅ 网格布局三列显示环境数据
- ✅ CardView卡片式设计

## 技术实现细节

### 新增文件

#### 1. MqttManager.java
MQTT客户端管理类，负责：
- 连接OneNET MQTT服务器（mqtts.heclouds.com:1883）
- 订阅主题：`$sys/{product_id}/{device_name}/property/post/reply`
- 发布主题：`$sys/{product_id}/{device_name}/thing/property/set`
- 消息接收回调
- 连接状态监听
- 自动重连机制

### 修改文件

#### 1. MainActivity.java
主要改动：
- 删除HTTP轮询任务（Runnable task）
- 删除GetOnenetData()方法（标记为@Deprecated）
- 删除DealJsonData()方法（标记为@Deprecated）
- 新增initMqttManager()方法
- 新增startOfflineDetection()方法
- 新增updateLastUpdateTime()方法
- 新增DealJsonDataFromMqtt()方法
- 修改controlDevice()方法使用MQTT发送指令
- 修改updateUI()方法遵循新规则

#### 2. build.gradle.kts
添加MQTT依赖：
```kotlin
implementation(libs.mqtt.client)
```

#### 3. libs.versions.toml
添加MQTT库版本：
```toml
[versions]
mqtt-client = "1.2.5"

[libraries]
mqtt-client = { group = "org.eclipse.paho", name = "org.eclipse.paho.client.mqttv3", version.ref = "mqtt-client" }
```

## 数据流程

### 接收数据流程
```
设备上报 → OneNET云平台 → MQTT Broker → APP订阅主题 → MqttManager回调 
→ DealJsonDataFromMqtt()解析 → 更新变量 → updateUI()刷新界面
```

### 发送控制指令流程
```
用户点击按钮 → controlDevice() → 本地优先更新界面 → mqttManager.publishCommand() 
→ MQTT Broker → OneNET云平台 → 设备接收
```

## 关键特性

### 1. 被动接收
- APP不主动请求数据
- 完全依赖设备上报
- 节省网络资源和电量

### 2. 智能离线检测
- 30秒无数据判定离线
- 每5秒检查一次
- 离线后禁用控制按钮

### 3. 本地优先
- 控制指令立即更新UI
- 提升用户体验
- 最终状态以设备上报为准

### 4. 数据持久性
- 收到的数据一直保持
- 不会因为未收到新数据而清空
- 初始状态显示"--"

## 测试建议

### 1. 基本功能测试
- [ ] APP启动后能成功连接MQTT
- [ ] 收到温度数据能正确显示
- [ ] 收到湿度数据能正确显示
- [ ] 收到光照数据能正确显示
- [ ] 告警状态能实时切换颜色

### 2. 离线检测测试
- [ ] 设备停止上报30秒后显示离线
- [ ] 离线后控制按钮置灰
- [ ] 离线后无法发送控制指令

### 3. 控制功能测试
- [ ] LED开关能正常发送指令
- [ ] 蜂鸣器开关能正常发送指令
- [ ] 控制后界面立即更新
- [ ] 设备状态上报后同步显示

### 4. 数据持久性测试
- [ ] 刚打开APP显示"--"
- [ ] 收到一次数据后保持显示
- [ ] 停止上报后数据不清空
- [ ] 最后更新时间正确显示

## 注意事项

1. **网络连接**：确保设备能访问mqtts.heclouds.com:1883
2. **权限配置**：需要在AndroidManifest.xml中添加网络权限
3. **产品ID和设备名**：确认PRODUCT_ID和DEVICE_NAME配置正确
4. **AccessKey**：确认ACCESS_KEY有效且未过期
5. **主题订阅**：确认订阅的主题格式正确

## 优势对比

### HTTP轮询方式（旧）
- ❌ 每2秒主动请求一次
- ❌ 浪费网络和电量
- ❌ 实时性差（最多2秒延迟）
- ❌ 服务器压力大

### MQTT方式（新）
- ✅ 被动接收，无需轮询
- ✅ 节省网络和电量
- ✅ 实时性强（毫秒级延迟）
- ✅ 服务器压力小
- ✅ 支持离线检测
- ✅ 更好的用户体验

## 总结

本次改造完全遵循了设备上传规则和APP开发规范，实现了：
1. MQTT被动接收，不做轮询
2. 环境数据收到一次更新一次，保持最后值
3. 告警状态实时监听，立即刷新
4. 最后更新时间只在收到新数据时更新
5. 30秒无数据判定离线，离线后按钮置灰
6. 初始状态显示"--"，收到后保持
7. 不做定时自动刷新，节省资源
8. 控制指令本地优先更新
9. 保持原版界面风格

改造后的APP更加高效、实时、节能，符合物联网应用的最佳实践。
