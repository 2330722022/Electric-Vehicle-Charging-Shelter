# OneNET 物联网监控 APP - 新增模块说明

## 📋 更新概述

在设备控制区下方、刷新按钮上方，成功新增两个功能模块：
1. **舵机状态卡片** - 显示舵机当前状态或角度
2. **告警状态详情卡片** - 与顶部告警状态联动，显示详细告警信息

---

## ✅ 已完成的修改

### 1. 布局文件 (activity_main.xml)

#### 新增模块位置
```
LinearLayout (垂直)
├── 1. 顶部标题区
├── 2. 告警状态区
├── 3. 数据卡片区 (GridLayout 3列)
├── 4. 更新时间
├── 5. 设备控制区
├── 6. 舵机状态卡片 ← 新增
├── 7. 告警状态详情卡片 ← 新增
└── 8. 刷新按钮
```

#### 6. 舵机状态卡片
- **样式**：与温度/湿度/光照卡片完全一致
- **背景**：白色 (#FFFFFF)
- **圆角**：12dp
- **阴影**：3dp
- **边距**：左右 12dp，底部 12dp
- **内容**：
  - 标题："舵机状态"（灰色 14sp）
  - 数值：`tvServoStatus`（紫色 #9C27B0，24sp，加粗）
  - 默认显示："--"
  - 居中显示

#### 7. 告警状态详情卡片
- **样式**：与顶部告警状态卡片风格统一
- **ID**：`cardAlarmDetail`
- **圆角**：12dp
- **阴影**：3dp
- **边距**：左右 12dp，底部 12dp
- **背景色动态切换**：
  - 正常状态：浅绿色 (#C8E6C9)
  - 告警状态：浅红色 (#FFCDD2)
- **内容**：
  - ID：`tvAlarmDetail`
  - 文字居中，加粗，16sp
  - 内边距：20dp
  - 文字颜色动态切换：
    - 正常：深绿色 (#2E7D32)
    - 告警：深红色 (#C62828)
  - 默认显示："当前状态：正常"

### 2. MainActivity.java

#### 新增控件引用
```java
private TextView servo_status_tv, alarm_detail_tv;
private CardView card_alarm_detail;
```

#### 新增 findViewById
```java
servo_status_tv = findViewById(R.id.tvServoStatus);
alarm_detail_tv = findViewById(R.id.tvAlarmDetail);
card_alarm_detail = findViewById(R.id.cardAlarmDetail);
```

#### 修改 updateUI() 方法

**告警状态同步逻辑**：
```java
if (alarm_state) {
    // 顶部告警卡片
    alarm_state_tv.setText("告警触发");
    card_alarm.setCardBackgroundColor(0xFFFFCDD2);
    alarm_state_tv.setTextColor(0xFFC62828);
    
    // 同步更新告警详情卡片
    alarm_detail_tv.setText("当前状态：倾斜告警");
    card_alarm_detail.setCardBackgroundColor(0xFFFFCDD2);
    alarm_detail_tv.setTextColor(0xFFC62828);
} else {
    // 顶部告警卡片
    alarm_state_tv.setText("正常");
    card_alarm.setCardBackgroundColor(0xFFC8E6C9);
    alarm_state_tv.setTextColor(0xFF2E7D32);
    
    // 同步更新告警详情卡片
    alarm_detail_tv.setText("当前状态：正常");
    card_alarm_detail.setCardBackgroundColor(0xFFC8E6C9);
    alarm_detail_tv.setTextColor(0xFF2E7D32);
}
```

**舵机状态更新**：
```java
// 更新舵机状态（示例：根据 tilt_angle 判断）
// 这里可以根据实际的舵机状态字段进行更新
servo_status_tv.setText("角度：0°");  // 默认值，可根据实际数据修改
```

---

## 🎨 界面效果

### 正常状态
```
┌─────────────────────────────────┐
│ 设备控制                         │
│ ┌────────┐ ┌────────┐          │
│ │ LED 开 │ │ LED 关 │          │
│ └────────┘ └────────┘          │
│ ┌────────┐ ┌────────┐          │
│ │蜂鸣器开│ │蜂鸣器关│          │
│ └────────┘ └────────┘          │
├─────────────────────────────────┤
│                                 │
│         舵机状态                 │ ← 白色卡片
│         角度：0° (紫色)          │
│                                 │
├─────────────────────────────────┤
│                                 │
│     当前状态：正常 (深绿色)      │ ← 浅绿色背景
│                                 │
├─────────────────────────────────┤
│                                 │
│       刷新数据 (蓝色按钮)        │
│                                 │
└─────────────────────────────────┘
```

### 告警状态
```
┌─────────────────────────────────┐
│ 设备控制                         │
│ ...（控制按钮）                  │
├─────────────────────────────────┤
│                                 │
│         舵机状态                 │
│         角度：45° (紫色)         │
│                                 │
├─────────────────────────────────┤
│                                 │
│   当前状态：倾斜告警 (深红色)    │ ← 浅红色背景
│                                 │
├─────────────────────────────────┤
│       刷新数据                   │
└─────────────────────────────────┘
```

---

## 🔧 自定义扩展

### 1. 舵机状态动态更新

如果您的 OneNET 物模型中有舵机相关字段（如 `servo_angle`、`servo_lock` 等），可以在 `DealJsonData()` 方法中添加解析逻辑：

```java
// 在 DealJsonData() 方法的 for 循环中添加
else if(identifier.equals("servo_angle")) {
    // 假设舵机角度字段为 servo_angle
    String angle = val;
    servo_status_tv.setText("角度：" + angle + "°");
}
else if(identifier.equals("servo_lock")) {
    // 假设舵机锁定状态字段为 servo_lock
    boolean isLocked = val.equals("1") || val.equalsIgnoreCase("true");
    servo_status_tv.setText(isLocked ? "已锁定" : "已解锁");
}
```

### 2. 告警详情自定义

根据不同类型的告警，可以显示不同的详细信息：

```java
if (alarm_state) {
    // 可以根据其他传感器数据判断具体告警类型
    if (tilt_angle > 30) {
        alarm_detail_tv.setText("当前状态：倾斜告警（角度过大）");
    } else if (vibration > threshold) {
        alarm_detail_tv.setText("当前状态：振动告警");
    } else {
        alarm_detail_tv.setText("当前状态：未知告警");
    }
}
```

### 3. 添加更多状态字段

如果需要显示更多设备状态，可以按照相同模式添加：

1. 在布局文件中添加新的 CardView
2. 在 MainActivity.java 中添加对应的 TextView 引用
3. 在 updateUI() 方法中更新显示内容

---

## 📊 样式一致性保证

### 卡片样式统一
| 属性 | 值 | 应用范围 |
|------|-----|---------|
| 圆角 | 12dp | 所有卡片 |
| 阴影 | 3dp | 所有卡片 |
| 左右边距 | 12dp | 所有卡片 |
| 底部边距 | 12dp | 所有卡片 |
| 内边距 | 16dp | 数据卡片 |
| 背景色 | #FFFFFF | 数据卡片、舵机卡片 |

### 字体层级统一
| 元素 | 大小 | 颜色 | 粗细 |
|------|------|------|------|
| 卡片标题 | 14sp | #757575 | 正常 |
| 数据数值 | 24-26sp | 彩色 | 加粗 |
| 单位 | 12sp | #9E9E9E | 正常 |
| 告警文字 | 16sp | 动态 | 加粗 |

---

## ✅ 验收清单

- [x] 舵机状态卡片位于设备控制区下方
- [x] 告警详情卡片位于舵机卡片下方、刷新按钮上方
- [x] 所有卡片样式与现有卡片一致（圆角、阴影、间距）
- [x] 告警详情卡片与顶部告警状态联动同步
- [x] 背景色和文字颜色随告警状态动态切换
- [x] 不影响原有功能（连接状态、数据显示、设备控制）
- [x] 编译无错误
- [x] 布局自适应屏幕

---

## 🔗 相关文件

- **布局文件**：`app/src/main/res/layout/activity_main.xml`
  - 第 324-360 行：舵机状态卡片
  - 第 362-383 行：告警状态详情卡片

- **主程序**：`app/src/main/java/com/example/onenet215/MainActivity.java`
  - 第 48-49 行：新增控件引用
  - 第 80-82 行：findViewById 初始化
  - 第 115-135 行：updateUI() 中的更新逻辑

---

## 💡 使用建议

### 1. 舵机状态数据来源
- 如果 OneNET 物模型中有舵机角度字段，建议在 `DealJsonData()` 中解析并更新
- 如果没有专门字段，可以根据 `tilt_angle` 或其他相关数据推算

### 2. 告警详情扩展
- 当前实现：简单显示"正常"或"倾斜告警"
- 可扩展：根据多个传感器数据综合判断，显示更详细的告警原因

### 3. 性能优化
- 两个新卡片只在数据更新时刷新，不影响性能
- 背景色切换使用 setColor()，效率高

---

**更新完成！** 🎉

新增模块已完美集成到现有布局中，保持了整体设计的一致性和美观性。
